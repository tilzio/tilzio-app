# Go
/bin/
*.test
*.out

# Wails build output (added in Plan 2)
/build/bin/

# Node / frontend (added in Plan 3)
node_modules/
frontend/node_modules
frontend/dist/

# Brainstorm visual-companion mockups
.superpowers/

# Wails generated TypeScript bindings (regenerated by `wails3 generate bindings`)
frontend/bindings/

# Task (go-task) cache, created by `wails3 build` / `wails3 dev`
.task/

# Wails-regenerated macOS packaging assets (built from appicon.png / appicon.icon)
build/darwin/Assets.car
build/darwin/icons.icns

# OS
.DS_Store

# Docs (brainstorm / plans / specs / kickoffs / API docs) — kept locally, not committed
docs/

# Claude service files (skills, launch config, mockups, results) — kept locally, not committed
.claude/

# Secrets / credentials — NEVER commit
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.keystore
.npmrc
.netrc
secrets.*
credentials.json
